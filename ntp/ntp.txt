# Using merged

- name: Merge provided configuration with device configuration
  rucdev.ix.ix_ntp:
    config:
      - address_families:
          - afi: ipv4
            enable: true
            access_list: ntp-acl
        servers:
          - address: 192.0.2.1
            timeout: 64
            version: 3
    state: merged

